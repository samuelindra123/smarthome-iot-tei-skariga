// Temporary shim so the editor type checker recognizes framer-motion.
// framer-motion already ships types; if this persists, ensure tsconfig includes node_modules.
// Remove this file if not needed later.
declare module 'framer-motion';
